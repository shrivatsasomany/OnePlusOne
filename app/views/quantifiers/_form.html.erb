<%= form_for(@quantifier) do |f| %>
  <% if @quantifier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quantifier.errors.count, "error") %> prohibited this quantifier from being saved:</h2>

      <ul>
      <% @quantifier.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
